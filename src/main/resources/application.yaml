spring:
  application:
    name: password-validation-service
  datasource:
    url: jdbc:mysql://localhost:3306/passworddb
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect