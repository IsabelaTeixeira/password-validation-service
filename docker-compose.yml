version: "3.9"

services:
  mysql:
    image: mysql:8
    container_name: password-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: passworddb
    ports:
      - "3306:3306"